services:
  postgres:
    image: 'postgres:latest'
    environment:
      POSTGRES_DB: mydatabase
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
    ports:
      - '5432'
    deploy:
      resources:
        limits:
          memory: 256M
